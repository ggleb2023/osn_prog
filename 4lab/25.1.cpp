#include <iostream>

using namespace std;

int main() {
    
    int x, y, z;
    cin >> x >> y >> z;
    
    if ((x + y * z) > x) min = x + y * z;
    else min = x;

    if ((x + y - z) > min) max = x + y - z;
    else max = min 
    cout << res;

}
